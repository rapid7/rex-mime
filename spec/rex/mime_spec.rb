require 'spec_helper'

describe Rex::Mime do
  it 'has a version number' do
    expect(Rex::Mime::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
